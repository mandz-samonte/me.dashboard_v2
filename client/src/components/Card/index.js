import React from 'react';

function Card(props) {
  const { className } = props;

  return (
    <div
      className={`${className} bg-white rounded-md`}
    >

    </div>
  )
}

export default Card;
